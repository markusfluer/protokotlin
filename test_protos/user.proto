syntax = "proto3";

package user;

message User {
    int32 id = 1;
    string username = 2;
    string email = 3;
    bool is_active = 4;
    repeated string roles = 5;
}

enum UserType {
    REGULAR = 0;
    ADMIN = 1;
    MODERATOR = 2;
}